spring:
  cloud:
    config:
      enabled: false

  flyway:
    enabled: true
    schemas: invoice
    default-schema: invoice
    locations: classpath:db/migration

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    org.hibernate.SQL: warn
